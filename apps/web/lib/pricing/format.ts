/**
 * Re-export of the shared pricing formatters. The canonical implementation
 * lives in `@kestrel/shared/pricing/format`, shared with apps/admin.
 */

export {
  formatGBP,
  formatGBPCompact,
  formatTierLabel,
  formatTierRange,
} from "@kestrel/shared/pricing/format";
